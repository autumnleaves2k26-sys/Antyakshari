{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "node -e \"const fs = require('fs'); fs.rmSync('package-lock.json', { force: true }); fs.rmSync('yarn.lock', { force: true });\"",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "vercel-build": "pnpm run typecheck:libs && pnpm --filter @workspace/antyakshari run build && rm -rf ./dist && cp -r artifacts/antyakshari/dist ./dist && cp ./dist/index.html ./dist/404.html",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck"
  },
  "private": true,
  "devDependencies": {
    "prettier": "^3.8.3",
    "typescript": "~5.9.3"
  }
}
